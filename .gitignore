node_modules/
dist/
.env
*.log
.DS_Store
devlog/
.tmp/
.opencode/

# Local agent/session artifacts
.codexclaw/
.omo/

# Test-generated artifacts
tests/.tmp-*/

# Codex session state (per AGENTS.md §0d.0 v9 schema - do NOT commit)
.codex-session*.json

# Pre-write backup snapshots (per AGENTS.md §7.0 - keep on disk, do NOT commit)
*.bak-*

# Python bytecode caches
__pycache__/

# Local scratch / transient scripts (ocx-start.py helpers, patches, etc.)
tmp/
